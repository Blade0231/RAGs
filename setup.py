from setuptools import setup

setup(
   name='RAG',
   version='1.0',
   description='RAG Module',
   author='Blade',
   author_email='',
   packages=['src'],  #same as name
   install_requires=['wheel'], 
)